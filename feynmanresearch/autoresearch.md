# Autoresearch: Causality improvement for CNN-Transformer exoplanet model

Started: 2026-06-05
Workspace: `/Users/aakashrajput/MachineLearning/Exoplanets/feynmanresearch`
Environment: local using `/Users/aakashrajput/MachineLearning/venvNLP`

## Goal

Improve causal behavior / causal generalization of the CNN-Transformer model without changing existing model code.

## Current constraint

User requested: do not change code. Therefore this session will only create research/evaluation artifacts in `feynmanresearch/` and will not edit `src/transformerarch/*` unless the user later approves.

## Primary metric

Because no explicit causal metric was provided, use a no-code proxy today:

- Primary: evaluation RMSE from `src/transformerarch/test.py` if available.
- Research target: identify how to measure `worst-environment RMSE` across nuisance environments such as `star_class`, `star_temperature`, `distance_parsec`, `surface_temperature`, and `surface_pressure`.
- Direction: lower is better.

## Benchmark command

```bash
source /Users/aakashrajput/MachineLearning/venvNLP/bin/activate
python src/transformerarch/test.py
```

## Files in scope

- Allowed: `feynmanresearch/*`
- Not allowed without further user approval: existing model/training code under `src/`.

## Iteration plan

1. Run baseline evaluation with the existing checkpoint.
2. Inspect output metrics and errors.
3. Recommend a same-day causal intervention that does not require model-code edits, prioritizing:
   - environment-based validation split;
   - input masking / ablation evaluation;
   - training-data balancing by environment;
   - config-only or data-split-only changes if user allows.

## pi-autoresearch tool status

The visible tool set in this session does not expose `init_experiment`, `run_experiment`, or `log_experiment`. I am emulating the loop with shell commands and durable logs in this folder.

## Baseline results

### Attempt 1

Failed because `torchinfo` was not installed in `/Users/aakashrajput/MachineLearning/venvNLP`.

Action taken: installed `torchinfo==1.8.0` in the selected virtual environment.

### Attempt 2

Failed because `src/transformerarch/test.py` uses MPS, while `torchinfo.summary(..., device="cpu")` appears to leave model weights on CPU. The evaluation then saw MPS inputs with CPU weights.

Action taken: created `feynmanresearch/run_transformer_test_cpu.py`, a wrapper that disables MPS for this run only. No source code was edited.

### Baseline evaluation

Source: `src/transformerarch/details.txt`, generated by existing test script.

- Average R2 Score: 0.7179
- Average RMSE: 0.0229
- Average MAE: 0.0173
- Evaluation subset size: 5000 validation samples

Note: although no source code was edited, the existing test script writes `src/transformerarch/details.txt` and chart PNGs under `src/transformerarch/charts/` as part of evaluation.

## Masking probe result

Created and ran `feynmanresearch/causal_mask_probe.py` on 512 validation samples.

This is an occlusion/robustness proxy, not proof of causality.

| Keep fraction of wavelength sequence | Overall RMSE |
|---:|---:|
| 1.00 | 0.024216 |
| 0.75 | 0.043381 |
| 0.50 | 0.053951 |
| 0.25 | 0.066217 |

Observation: removing the right-side/later wavelength context substantially increases RMSE. Strongest sensitivity is visible in `CO2`, `O2`, and `N2`.

## Best no-code thing to try today

Do not add a causal Transformer mask yet. For spectra, a causal time mask is probably the wrong first move because wavelength positions are not time steps and bidirectional spectral context is physically useful.

Try this first:

1. Create environment-based validation splits using nuisance variables from `data/summary.csv`:
   - `star_class`
   - `star_temperature` quantiles
   - `distance_parsec` quantiles
   - `surface_temperature` quantiles
   - `surface_pressure` quantiles
2. Report worst-environment RMSE and random-validation RMSE gap.
3. Only if worst-environment RMSE is poor, try data balancing or environment-weighted sampling before changing the model.

Rationale: this tests whether the model is learning stable spectral mechanisms rather than shortcuts tied to random train/val splits.

## Environment RMSE probe

Created and ran `feynmanresearch/environment_rmse_probe.py` on 1024 reconstructed validation samples.

Important caveat: it reconstructs the random validation split using the current `os.listdir` order and seed from `dataloader.py`; save explicit indices before treating this as publication-grade.

- Overall RMSE: 0.023069
- Worst tested environment: `distance_parsec` bin `(11.532, 15.0]`
- Worst-environment RMSE: 0.025660
- Gap vs overall: +0.002591

Interpretation: the largest observed environment weakness today is the far-distance bin. That suggests a practical no-model-code next try: balance or weight training samples by `distance_parsec` bins, or evaluate a held-out-distance split before trying causal attention masks.
